.header
  Welcome to the Lists Manager

- if @lists.present?
  %ul
    - @lists.each do |list|
      %li
        = link_to 'edit', edit_list_path(list)
        |
        = list.subject
- else
  No lists.



.actions
  = link_to 'New List', new_list_path
